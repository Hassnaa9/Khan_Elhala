import 'package:flutter/material.dart';

class Category {
  final String name;
  final String icon; // Using IconData for simplicity, but you can use image paths

  Category({required this.name, required this.icon});
}